export interface Resident {
    id: string;
    name: string;
    age: number;
    phone: string;//most types are string because manipulating is easy in string
    checkInDate: string;
    roomNumber: number;

}